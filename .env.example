# ── Vercel 배포 시에만 필요합니다. 로컬은 이 파일 없이 그대로 동작합니다. ──

# Postgres (Vercel Marketplace → Neon 연동 시 자동 주입됩니다)
# POSTGRES_URL="postgres://user:password@host/db?sslmode=require"

# Vercel Blob (Storage → Blob 생성 시 자동 주입됩니다)
# BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."
